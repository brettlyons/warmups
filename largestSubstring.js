function lgstSubstring(string1, string2) {
  var i = 0;
  var j = 0; //longest distance between i and j == largest substring

  while (i < longestStr(string1, string2).length ) {

  }

  return string1.match(string2);

  function longestStr ( string1, string2 ) {
    return (string1.length > string2.length) ? string1 : string2;
  }
}
